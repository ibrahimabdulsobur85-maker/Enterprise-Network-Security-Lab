# Final Network Testing

## Objective

The completed network was tested to confirm that the configured
networking and security features operate as intended.

```text

## 1. Inter-VLAN Connectivity

Connectivity between permitted VLANs was tested.

## Tests

- VLAN 10 → VLAN 20: PASS
- VLAN 10 → VLAN 30: PASS
- VLAN 10 → VLAN 40: BLOCKED by ACL as intended

The successful tests confirmed that inter-VLAN routing is working
for permitted traffic.

## 2. ACL Verification

Traffic from VLAN 10 toward the Server VLAN (VLAN 40) was tested.

### Result

```text
Destination host unreachable

## 3. WAN Connectivity

An internal PC successfully communicated with the ISP router
through the Company Router.

Result: PASS

## 4. NAT Verification

NAT translations were generated by internal traffic and verified
using:

show ip nat translations

The translation table showed internal private addresses being
translated through the Company's WAN interface.

Result: PASS

## 5. DHCP and DHCP Snooping

DHCP bindings were verified on the Company Router using:

show ip dhcp binding

DHCP Snooping bindings were verified on the switch using:

show ip dhcp snooping binding

Result: PASS

## 6. Port Security

Port security was verified using:

show port-security interface fa0/2

The interface showed that port security was enabled with a
maximum of one secure MAC address and the configured violation
mode.

Result: PASS

## 7. STP Security

PortFast and BPDU Guard were verified on the access interface.

show spanning-tree interface fa0/2 detail
show running-config interface fa0/2

The interface was confirmed to be operating in PortFast mode
and BPDU Guard was configured.

Result: PASS

## 8. SSH Management

SSH remote management was tested from a network PC.

The PC successfully established an SSH session with the
Company Router.

Result: PASS
